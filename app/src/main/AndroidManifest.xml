<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alisherka7.attendanceproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AttendanceProject"
        android:usesCleartextTraffic="true">
        <activity android:name=".LessonAttendActivity"></activity>
        <activity android:name=".ProfessorLecturesActivity" />
        <activity android:name=".ProfessorProfileActivity" />
        <activity android:name=".ProfessorLoginActivity" />
        <activity android:name=".LectureActivity" />
        <activity android:name=".AttendanceActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>